# 旧版前端备份（ReadAloud 1.x）

合并 **ReadAloud 2.0**（原 `readaloud-home-ui`）到仓库根目录 `src/` 时，将此前的根目录 `src/` 完整复制到本目录 `readaloud-1.x-src/`。

- 分支：`feature/readaloud2.0`
- 恢复方式：将 `readaloud-1.x-src/` 内文件拷回根目录 `src/`（并恢复对应 `index.html` / `vite.config` / 依赖），或从 Git 历史中检出合并前的提交。
